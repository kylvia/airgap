# airgap is itself a secret-scanning tool; test/ contains synthetic API-key
# samples (all share the same fabricated token body) used to verify the
# detector's rule table. They are not real credentials. Exclude them so
# secret scanning does not raise noise alerts on the project's own fixtures.
paths-ignore:
  - "test/**"
